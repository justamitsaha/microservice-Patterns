apiVersion: apps/v1
kind: Deployment
metadata:
  name: reactiveorderservice
  labels:
    app: reactiveorderservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reactiveorderservice
  template:
    metadata:
      labels:
        app: reactiveorderservice
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/actuator/prometheus"
    spec:
      containers:
        - name: app
          image: $IMAGE_REPO/new-ms-reactive-order-service:$IMAGE_VERSION
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: microservice-app-settings
          env:
            - name: SPRING_APP_NAME
              value: order-service
            - name: SERVER_PORT
              value: "8080"
            - name: SPRING_ACTIVE_PROFILE
              value: gcp
            - name: SPRING_R2DBC_USERNAME
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: mysql-user
            - name: SPRING_R2DBC_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: mysql-password
            - name: OTEL_SERVICE_NAME
              value: reactiveorderservice
        - name: alloy
          image: grafana/alloy:main
          args: ["run", "/etc/alloy/alloy.alloy"]
          env:
            - name: ALLOY_ENABLE_FEATURES
              value: flow
          ports:
            - name: otlp-http
              containerPort: 4318
            - name: otlp-grpc
              containerPort: 4317
          volumeMounts:
            - name: alloy-config
              mountPath: /etc/alloy
            - name: varlog
              mountPath: /var/log
      volumes:
        - name: alloy-config
          configMap:
            name: alloy-config
        - name: varlog
          hostPath:
            path: /var/log
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: reactiveorderservice
  labels:
    app: reactiveorderservice
spec:
  selector:
    app: reactiveorderservice
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
