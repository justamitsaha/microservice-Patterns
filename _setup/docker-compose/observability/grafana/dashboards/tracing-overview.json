{
  "id": null,
  "title": "Tracing Overview",
  "tags": ["tracing", "tempo"],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "panels": [
    {
      "type": "text",
      "title": "Trace Search",
      "gridPos": {"h": 6, "w": 24, "x": 0, "y": 0},
      "options": {
        "mode": "markdown",
        "content": "Use Grafana Explore with the Tempo datasource to search traces by service name (order-service, customer-service, gateway-service). Switch to the 'TraceQL' tab to run advanced queries."
      }
    },
    {
      "type": "timeseries",
      "title": "Request Rate with Exemplars",
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 6},
      "options": {"legend": {"showLegend": true}},
      "targets": [
        {
          "expr": "sum(rate(http_server_requests_seconds_count[1m])) by (job)",
          "legendFormat": "{{job}}",
          "exemplar": true
        }
      ]
    }
  ]
}

